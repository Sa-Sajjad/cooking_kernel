version: 2.1
jobs:
  compile:
   docker:
      - image: gitpod/workspace-full:latest
      # - image: ubuntu:22.04
   steps:
      - run:
          command: |
           git config --global user.name "Sã Śâjjãd"; git config --global user.email "103353616+Sa-Sajjad@users.noreply.github.com"; git config --global color.ui false
           git clone --depth=1 https://Sa-Sajjad:$ght@github.com/Sa-Sajjad/kernel_xiaomi_lavender-4.19 -b susfs kernel && cd kernel
           # sudo rm /etc/apt/sources.list.d/ondrej-ubuntu-nginx-mainline-jammy.list
           bash build.sh -up

workflows:
  version: 2.1
  cooking:
    jobs:
      - compile
