jobs:
  build:
    docker:
      - image: 31482281/codespace:latest
    # resource_class: large
    steps:
      - checkout
      - run:
         name: Run script to start building kernel
         command: ls && git clone --depth=1 https://Snax-phycho:$ght@github.com/Snax-phycho/kernel_xiaomi_tulip -b lav snx && cd snx && git clone https://Snax-phycho:$ght@Snax-phycho/cooking_kernel.git -b unitrix ck && bash ck/build.sh